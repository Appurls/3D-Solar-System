# fastapi==0.115.6
# uvicorn[standard]==0.32.1
# skyfield==1.49
# numpy==2.1.3
python3 -m venv .venv
source .venv/bin/activate
pip install fastapi uvicorn skyfield numpy
uvicorn app.main:app --reload
